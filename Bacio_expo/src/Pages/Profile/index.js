import React from 'react';
import {Text} from 'react-native'

export default function Profile ({route,navigation}){

    const { clasId,prods } = route.params

    return(
        <Text>Profile</Text>

    )
}